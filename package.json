{
  "name": "catalyst-app",
  "version": "1.4.3",
  "description": "FrcCatalyst desktop companion (Tauri)",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test \"src/**/*.test.js\"",
    "tauri": "tauri",
    "bundle-console": "node scripts/bundle-console.mjs",
    "build:all": "node scripts/bundle-console.mjs && tauri build",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
