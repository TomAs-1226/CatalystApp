{
  "name": "catalyst-app",
  "version": "2.6.1",
  "description": "FrcCatalyst desktop companion (Tauri)",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node scripts/sync-tools.mjs --check && node --test \"src/**/*.test.js\"",
    "tauri": "tauri",
    "bundle-console": "node scripts/bundle-console.mjs",
    "bundle-docs": "node scripts/bundle-docs.mjs",
    "prepare-resources": "node scripts/sync-tools.mjs && node scripts/bundle-console.mjs && node scripts/bundle-docs.mjs",
    "dev": "tauri dev",
    "build": "npm run prepare-resources && tauri build",
    "build:all": "npm run build",
    "sync-tools": "node scripts/sync-tools.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
